:_mod-docs-content-type: CONCEPT
include::_attributes/common-attributes.adoc[]
[id="configure-webhook-secrets-git-providers"]
= Configure webhook secrets for Git providers

You can configure webhook secrets for Git providers declaratively by using the `Argo CD` custom resource (CR) to manage webhook credentials alongside your GitOps configuration instead of manually updating the `argocd-secret` secret.

